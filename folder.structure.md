<!-- src/
 ├── app/              # routes & UI
 ├── components/       # reusable components
 ├── context/          # React context providers
 ├── contents/ (or data) # static content like blog/project metadata
 ├── types/            # TypeScript types
 └── utils/            # helper functions, animations, utils
public/
 └── projects/         # static images/assets -->
